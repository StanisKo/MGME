import { ReactElement } from 'react';

import { Box, Typography } from '@material-ui/core';

export const ItemOne = (): ReactElement => (
    <Box>
        <Typography>
            Item One
        </Typography>
    </Box>
);
